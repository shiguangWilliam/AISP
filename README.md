# MediSage 医疗智能助手

这是一个为非计算机专业用户准备的“傻瓜式”快速上手教程，帮助您在本地运行 MediSage 前端界面。

---

## 🚀 快速上手教程（小白专用）

### 第一步：安装运行环境
您的电脑需要安装一个名为 **Node.js** 的软件才能运行这个网页。
1. 打开 [Node.js 官网](https://nodejs.org/)。
2. 点击左侧的 **"LTS"** 版本（长期支持版）进行下载并安装。
3. 安装过程一路点击“下一步”即可。

### 第二步：下载并打开项目
1. 将本项目下载到您的电脑上并解压。
2. 记住文件夹的位置（例如：`C:\Users\您的名字\Desktop\MediSage`）。

### 第三步：打开控制台（黑窗口）
1. 在 Windows 搜索栏输入 `cmd` 或 `PowerShell` 并打开。
2. 输入以下命令进入项目文件夹（请将路径替换为您实际的文件夹路径）：
   ```bash
   cd C:\Users\您的名字\Desktop\MediSage\forntend
   ```
   *注意：如果您的文件夹在 D 盘，请先输入 `D:` 然后回车，再输入 `cd ...`*

### 第四步：安装必要的组件
在黑窗口中输入以下命令并回车：
```bash
npm install
```
*这可能需要几分钟时间，请耐心等待进度条走完。如果看到 "added ... packages"，说明成功了。*

### 第五步：启动网页
在黑窗口中输入以下命令并回车：
```bash
npm run dev
```
*看到 "Ready in ..." 或 "Local: http://localhost:3000" 的字样时，说明程序已经跑起来了！*

### 第六步：访问界面
1. 打开您的浏览器（推荐使用 Chrome 或 Edge）。
2. 在地址栏输入：`http://localhost:3000`
3. 现在您就可以看到 MediSage 的界面并开始使用了！

---

## 💡 常见问题
- **问：黑窗口可以关掉吗？**
  - 答：不可以。在使用网页期间，请保持黑窗口开启。如果您想停止运行，可以在黑窗口按 `Ctrl + C`。
- **问：下次怎么启动？**
  - 答：下次使用时，只需重复“第三步”和“第五步”即可。
- **问：报错了怎么办？**
  - 答：请检查 Node.js 是否安装成功，或者尝试删除 `forntend` 文件夹下的 `node_modules` 文件夹后重新执行“第四步”。

---

## 🛠️ 开发者信息
本项目基于 **Next.js** 开发。
- 目录结构：`forntend/` 包含所有前端代码。
- 数据存储：`src/data/` 包含模拟的 JSON 数据。
